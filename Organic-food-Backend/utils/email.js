    export const sendEmail = async (to, subject, text, html) => {
        // Configure your email service (e.g., Nodemailer, SendGrid, etc.)
        console.log(`Sending email to ${to}: ${subject}`);
    };
